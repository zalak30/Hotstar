import mysql.connector
import sqlite3
import conn

# # create database in sql
# mydb = mysql.connector.connect(
#   host=conn.host,
#   user=conn.user,
#   passwd=conn.passwd
# )
#
# mycursor = mydb.cursor()
#
# mycursor.execute("CREATE DATABASE videos")
